# isEmpty

`isEmpty(array)`

Returns `true` if `array` has no elements, otherwise returns `false`.

::: code-group

```ts [data-first]
isEmpty([]); // true
```

```ts [data-last]
pipe([], isEmpty()); // true
```

:::
