# length

`length(target)`

Returns the length of `target` string.

::: code-group

```ts [data-first]
length("hello world"); // 11
```

```ts [data-last]
pipe("hello world", length()); // 11
```

:::
